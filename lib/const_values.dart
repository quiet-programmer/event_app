import 'package:flutter/material.dart';

// colors for primary and accent
const Color mainColor = Color.fromRGBO(16, 38, 51, 1);
const Color subColor = Color.fromRGBO(253, 205, 1, 1);

const Color underColor = Color.fromRGBO(42, 65, 79, 1);
